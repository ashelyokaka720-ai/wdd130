/* Reset & universal selector */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --primary-color: #2c7a7b;   /* teal shade */
  --secondary-color: #f6ad55; /* light orange */
  --heading-font: "Georgia", serif;
  --body-font: Arial, sans-serif;
  --text-color: #264653;
  --bg-color: #edf2f4;
}

body {
  font-family: var(--body-font);
  color: var(--text-color);
  background-color: var(--bg-color);
  line-height: 1.6;
}

header, main, footer {
  width: 840px;
  margin: 0 auto;
}

header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 0;
  background-color: var(--primary-color);
  color: white;
}

header img {
  height: 60px;
}

nav ul {
  list-style: none;
}

nav ul li {
  display: inline-block;
  margin-left: 20px;
}

nav ul li a {
  text-decoration: none;
  color: white;
  font-weight: bold;
}

.hero {
  position: relative;
  margin: 30px 0;
}

.hero img {
  display: block;
  width: 100%;
  height: auto;
}

.hero h1 {
  position: absolute;
  top: 20px;
  width: 100%;
  text-align: center;
  color: white;
  background-color: rgba(44, 122, 123, 0.7); /* semi-transparent overlay */
  padding: 10px 0;
  font-family: var(--heading-font);
}

.hero article {
  position: absolute;
  bottom: 20px;
  left: 20px;
  background-color: rgba(255,255,255,0.8);
  padding: 15px;
  max-width: 40%;
}

.hero article img {
  float: right;
  width: 150px;
  height: auto;
  margin-left: 15px;
  border: 2px solid var(--primary-color);
}

section {
  margin: 30px 0;
}

section h2 {
  font-family: var(--heading-font);
  color: var(--primary-color);
  margin-bottom: 10px;
}

section p {
  font-size: 1rem;
}

footer {
  text-align: center;
  padding: 20px 0;
  border-top: 1px solid #ccc;
  margin-top: 40px;
  background-color: #ffffff;
}

.socialmedia a {
  margin: 0 10px;
}

.socialmedia img {
  width: 24px;
  height: auto;
}
